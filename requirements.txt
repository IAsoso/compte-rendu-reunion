# Synthia — dépendances Python
# (Transcription déportée sur l'API Groq : plus de Whisper/torch en local.)
#
# FFmpeg (utilisé par pydub pour décoder/découper l'audio) est fourni par le
# paquet pip imageio-ffmpeg : aucune installation système n'est nécessaire.
# main.py pointe pydub vers ce binaire embarqué au démarrage.

# --- API web ---
fastapi==0.138.1
uvicorn==0.49.0
python-multipart==0.0.32     # upload de fichiers (UploadFile / Form)
pydantic==2.13.4

# --- IA ---
groq==1.5.0                  # transcription (Whisper large v3 turbo via API)
google-genai==2.10.0         # nettoyage, résumé, actions, chat (Gemini)

# --- Authentification (comptes email / mot de passe) ---
passlib==1.7.4               # gestion du hachage des mots de passe
bcrypt==4.0.1                # algorithme de hachage (épinglé : évite un warning passlib avec bcrypt >= 4.1)
PyJWT==2.10.1                # jetons JWT signés (connexion sans session serveur)
email-validator==2.2.0       # validation du format email (via Pydantic EmailStr)
google-auth==2.55.1          # vérification des jetons d'identité Google (OAuth)
httpx==0.28.1                # appels HTTP (API Resend pour les emails transactionnels)

# --- Audio ---
pydub==0.25.1                # découpage des longues réunions
audioop-lts==0.2.2           # backport du module audioop (requis en Python 3.13+)
imageio-ffmpeg==0.6.0        # binaire FFmpeg embarqué (pas d'install système requise)

# --- Configuration ---
python-dotenv==1.2.2         # lecture du fichier .env (clés d'API)

# --- Paiement (abonnements) ---
stripe==15.3.0                # Checkout, Customer Portal, webhooks
